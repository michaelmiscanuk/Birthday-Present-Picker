const config = {
  /**
   * When true: shows the "Aktualizuje se každých 10 s" label and
   * the "Resetovat vše" button at the bottom of the page.
   * Set to false before going live.
   */
  test_mode: false,
};

export default config;
